import React from 'react';
import './box.css';

interface BoxProps {
    backgroundColor: string,
    borderColor: string,
    size: 'small' | 'medium' | 'large';
}

export const Box = ({
}: BoxProps) => {
    return (
        <></>
    );
};
  
